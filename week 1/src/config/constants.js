/**
 * Application Core Configuration Constants
 */
const CONFIG_CONSTANTS = Object.freeze({
  API_PREFIX: '/api',
  API_VERSION: 'v1',
  FULL_API_BASE: '/api/v1',
  DEFAULT_PORT: 3000,
  DEFAULT_JWT_EXPIRES_IN: '24h',
});

module.exports = CONFIG_CONSTANTS;
